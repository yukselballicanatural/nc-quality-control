/** @type {import('next').NextConfig} */
const nextConfig = {
  // Static asset caching for faster repeat loads
  async headers() {
    return [
      {
        source: '/_next/static/:path*',
        headers: [
          { key: 'Cache-Control', value: 'public, max-age=31536000, immutable' },
        ],
      },
    ]
  },
}

export default nextConfig
